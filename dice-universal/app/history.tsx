import {View, Text} from "react-native";

export default function HistoryScreen() {
    return (
        <View style={{flex: 1, padding: 16}}>
            <Text style={{fontSize: 18, fontWeight: "600"}}>Historique (à construire)</Text>
            <Text style={{marginTop: 8}}>Ici : listes des jets, résumé des règles, filtre par profil</Text>
        </View>
    );
}